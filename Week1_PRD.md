# 剣玉技辞典 Week 1 技術仕様書

## プロジェクト概要

**プロジェクト名:** Zen Kendama（剣玉技辞典）
**開発期間:** Week 1（5-7日間）
**目標:** MVP（Minimum Viable Product）として、技の閲覧・検索・投稿機能を実装

---

## 技術スタック

### フロントエンド
- **Framework:** Next.js 15 (App Router)
- **Language:** TypeScript
- **Styling:** TailwindCSS
- **多言語:** next-intl
- **認証:** NextAuth.js (Auth.js)
- **状態管理:** React hooks (useState, useContext)
- **フォームバリデーション:** Zod

### Week 1のデータソース
- **モックデータ:** JSON ファイル (`data/tricks.json`)
- Week 2でGoバックエンド + PostgreSQLに移行

---

## データ構造

### Trick（技）

```typescript
interface Trick {
  id: string;                    // 一意ID
  slug: string;                  // URL用スラッグ（例: "lighthouse"）
  nameJa: string;                // 技名（日本語）
  nameEn: string;                // 技名（英語）
  category: Category;            // カテゴリー
  subcategory: string;           // サブカテゴリー（例: "Basic Catch"）
  difficulty: 1 | 2 | 3 | 4 | 5; // 難易度（1:初級 〜 5:達人）
  difficultyLabel: string;       // 難易度ラベル（"初級", "中級", "上級"）
  attribute: string;             // 属性（例: "静止(Balance)", "回転(Rotation)"）
  thumbnailUrl: string;          // サムネイル画像URL
  iconUrl: string;               // カテゴリーアイコンURL
  tags: string[];                // タグ（例: ["初心者向け", "バランス技"]）
  videos: Video[];               // この技の動画投稿一覧
  createdAt: string;             // 登録日時（ISO 8601）
}

type Category = "大皿系" | "小皿系" | "中皿系" | "灯台系" | "飛行機系" | "とめけん系" | "回転系" | "糸技系";
```

### Video（動画投稿）

```typescript
interface Video {
  id: string;                    // 一意ID
  trickId: string;               // 技ID（外部キー）
  userId: string;                // 投稿者ID
  username: string;              // 投稿者名
  userAvatar: string;            // 投稿者アバターURL
  videoUrl: string;              // 動画URL（YouTube/Instagram/TikTok or 独自）
  videoType: "youtube" | "instagram" | "tiktok" | "upload";
  thumbnailUrl: string;          // 動画サムネイル
  comment?: string;              // コメント（任意）
  views: number;                 // 再生回数
  likes: number;                 // いいね数
  createdAt: string;             // 投稿日時
}
```

### User（ユーザー）

```typescript
interface User {
  id: string;
  email: string;
  username: string;
  avatar: string;
  provider: "google" | "email";  // 認証プロバイダー
  createdAt: string;
}
```

---

## ページ構成（ルーティング）

### 日本語ルート（`/`）

| ルート | ファイルパス | 説明 |
|--------|-------------|------|
| `/` | `app/page.tsx` | ホーム（技一覧） |
| `/tricks/[slug]` | `app/tricks/[slug]/page.tsx` | 技詳細ページ |
| `/search` | `app/search/page.tsx` | 技検索ページ |
| `/post/new` | `app/post/new/page.tsx` | 新技登録ページ |
| `/post/success` | `app/post/success/page.tsx` | 投稿成功ページ |
| `/auth/signin` | `app/auth/signin/page.tsx` | ログインページ |
| `/auth/signup` | `app/auth/signup/page.tsx` | 新規登録ページ |

### 英語ルート（`/en`）

| ルート | ファイルパス | 説明 |
|--------|-------------|------|
| `/en` | `app/[locale]/page.tsx` | ホーム（英語版） |
| `/en/tricks/[slug]` | `app/[locale]/tricks/[slug]/page.tsx` | 技詳細（英語版） |
| 以下同様... | | |

**next-intl 設定:**
- `middleware.ts` でロケール検出
- `app/[locale]/layout.tsx` で多言語レイアウト

---

## コンポーネント設計

### 共通コンポーネント

```
components/
├── layout/
│   ├── Header.tsx           # ヘッダー（ロゴ、言語切替、ハンバーガーメニュー）
│   ├── BottomNav.tsx        # ボトムナビゲーション（一覧、検索、設定）
│   └── Footer.tsx           # フッター
│
├── tricks/
│   ├── TrickCard.tsx        # 技カード（一覧用）
│   ├── TrickGrid.tsx        # 技カードのグリッド
│   ├── CategoryTabs.tsx     # カテゴリータブ
│   ├── VideoGrid.tsx        # 動画グリッド（2x2）
│   └── VideoCard.tsx        # 動画カード（ユーザー情報付き）
│
├── forms/
│   ├── TrickSearchBar.tsx   # 技検索バー（オートコンプリート）
│   ├── NewTrickForm.tsx     # 新技登録フォーム
│   ├── VideoUrlInput.tsx    # 動画URL入力フィールド
│   └── CategorySelector.tsx # カテゴリー選択UI
│
├── auth/
│   ├── SignInForm.tsx       # ログインフォーム
│   └── SignUpForm.tsx       # 新規登録フォーム
│
└── ui/
    ├── Button.tsx           # 共通ボタン
    ├── Badge.tsx            # バッジ（難易度、カテゴリー）
    ├── Spinner.tsx          # ローディングスピナー
    ├── Modal.tsx            # モーダル
    └── EmptyState.tsx       # 空状態表示
```

---

## 画面仕様

### 1. ホーム（技一覧）

**ファイル:** `app/page.tsx`

**表示要素:**
- ヘッダー（"けん玉辞典" / "Zen Kendama"、ハンバーガーメニュー）
- カテゴリータブ（全て、大皿、小皿、中皿、飛行機、灯台、とめけん）
- 技カードグリッド（3列 or 2列、レスポンシブ）
- ボトムナビゲーション（一覧、検索、設定）

**技カード仕様:**
- サムネイル画像（3D剣玉ビジュアル or 実写）
- 再生ボタン（中央）
- 左上：難易度バッジ（"初級", "中級", "上級"）
- 右上：ブックマークアイコン（Week 2実装、Week 1は表示のみ）
- 下部：技名（日本語大、英語小）
- サブカテゴリー（"Basic Catch"など）
- 右側：縦書き日本語（カテゴリー名）

**インタラクション:**
- カテゴリータブクリック → フィルター
- 技カードクリック → `/tricks/[slug]` に遷移

**ローディング:**
- 初回読み込み：シンプルなスピナー表示

**空状態:**
- カテ�ョリーに技がない場合：「この技カテゴリーはまだ登録されていません」

---

### 2. 技詳細ページ（比較台帳）

**ファイル:** `app/tricks/[slug]/page.tsx`

**表示要素:**
- 戻るボタン（左上）
- 技名（大きく表示：「灯台 Lighthouse」）
- 難易度・属性（「中級 (Intermediate)」「静止 (Balance)」）
- 動画グリッド（2x2、最大4件を優先表示）
- 下部：「コツを見る（Tips）」ボタン（Week 2実装）
- 固定ボトムバー：「この技を投稿する」ボタン

**動画カード仕様:**
- 動画サムネイル
- 再生ボタン（中央）
- 左上：ユーザーアバター（小）
- 下部：ユーザー名（@username）
- 右下：音量ミュートアイコン

**インタラクション:**
- 動画カードクリック → その場で動画再生（モーダル or インライン）
- 「この技を投稿する」ボタン → 認証チェック → `/post/new?trick=[slug]` に遷移

**空状態:**
- 動画が0件の場合：
  ```
  「まだ誰も投稿していません。
   最初の投稿者になりましょう！」
  + 「投稿する」ボタン
  ```

**ローディング:**
- 動画読み込み中：スピナー表示

---

### 3. 技検索ページ

**ファイル:** `app/search/page.tsx`

**表示要素:**
- 戻るボタン
- ページタイトル：「技の検索」
- 検索バー：「どの技を決めましたか？」
- オートコンプリート候補リスト
  - アイコン（カテゴリー別）
  - 技名（日本語大、英語小）
  - カテゴリーバッジ（"基本", "糸技", "刺し"）
- 最近の記録セクション（横スクロール）
- 「見つからない場合」リンク → `/post/new`

**インタラクション:**
- 入力中：リアルタイムでフィルタリング
- 候補クリック → その技の詳細ページへ遷移
- 「新しい技を登録」リンク → `/post/new`

**検索ロジック:**
- 日本語名、英語名、カテゴリー名で部分一致
- ふりがな対応（ひらがな入力でも検索可能）

---

### 4. 新技登録ページ

**ファイル:** `app/post/new/page.tsx`

**表示要素:**
- ヘッダー：「新技登録」
- キャンセルボタン（右上）
- 統計表示：「今月 142 個の技が被露されました」
- セクション1: 動機付けメッセージ
  ```
  「職人として貢献する」
  独自の流儀を共有し、仲間の技を磨きましょう。
  ```
- セクション2: 技の名称
  - 入力フィールド（例: 蹴裏回転 (Kickflip)）
  - 編集アイコン
- セクション3: 分類
  - 選択チップ（回転系、摺足系、静止系）
- セクション4: 修練度
  - スライダー（初級、中級、上級、達人）
- セクション5: 映像の記録
  - 「動画を読み込む」エリア
  - 注釈：「60秒以内・高画質推奨」
- セクション6: コミュニティフィード
  - 最近の投稿例（ソーシャルプルーフ）
- 「技を登録する ✨」ボタン

**フォームバリデーション（Zod）:**

```typescript
const newTrickSchema = z.object({
  nameJa: z.string().min(1, "技名（日本語）を入力してください"),
  nameEn: z.string().min(1, "技名（英語）を入力してください"),
  category: z.enum(["回転系", "摺足系", "静止系", /* ... */]),
  difficulty: z.number().min(1).max(5),
  videoUrl: z.string().url("有効なURLを入力してください")
    .refine((url) => isValidVideoUrl(url), "YouTube/Instagram/TikTokのURLを入力してください")
});
```

**インタラクション:**
- 動画URL入力後 → プレビュー自動表示
- バリデーションエラー → フィールド下に赤文字メッセージ
- 送信成功 → `/post/success?trick=[slug]` に遷移

**エラーハンドリング:**
- URL無効 → 「有効な動画URLを入力してください」
- 動画60秒超 → 「動画は60秒以内にしてください」
- 技名重複 → 「この技は既に登録されています」

---

### 5. 投稿成功ページ

**ファイル:** `app/post/success/page.tsx`

**表示要素:**
- チェックマークアイコン（緑、大きく）
- 「投稿完了！🎉」ヘッドライン
- 投稿した技の名前表示
- 動画サムネイル（小）
- 「技詳細ページを見る」ボタン

**インタラクション:**
- 3秒後に自動リダイレクト → 技詳細ページ
- または手動で「技詳細ページを見る」ボタンクリック

**Week 2で追加予定:**
- SNSシェアボタン（Instagram, TikTok, X）
- バッジ獲得表示

---

### 6. ログイン/新規登録ページ

**ファイル:** `app/auth/signin/page.tsx`, `app/auth/signup/page.tsx`

**NextAuth.js 設定:**

```typescript
// app/api/auth/[...nextauth]/route.ts
import NextAuth from "next-auth";
import GoogleProvider from "next-auth/providers/google";
import CredentialsProvider from "next-auth/providers/credentials";

export const authOptions = {
  providers: [
    GoogleProvider({
      clientId: process.env.GOOGLE_CLIENT_ID!,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET!
    }),
    CredentialsProvider({
      name: "Email/Password",
      credentials: {
        email: { label: "Email", type: "email" },
        password: { label: "Password", type: "password" }
      },
      async authorize(credentials) {
        // Week 1: モック認証
        // Week 2: Goバックエンドと接続
        if (credentials?.email === "test@example.com" && credentials?.password === "password") {
          return { id: "1", email: credentials.email, name: "Test User" };
        }
        return null;
      }
    })
  ],
  pages: {
    signIn: "/auth/signin",
  },
  session: {
    strategy: "jwt"
  }
};

const handler = NextAuth(authOptions);
export { handler as GET, handler as POST };
```

**ログイン画面仕様:**
- 「Zen Kendama にログイン」ヘッダー
- Google ログインボタン（大きく、アイコン付き）
- 区切り線「または」
- メールアドレス入力
- パスワード入力
- 「ログイン」ボタン
- 「アカウントをお持ちでない方」→ 新規登録リンク

**新規登録画面仕様:**
- 「Zen Kendama に登録」ヘッダー
- Google 登録ボタン
- 区切り線「または」
- ユーザー名入力
- メールアドレス入力
- パスワード入力
- パスワード確認入力
- 「登録」ボタン
- 「すでにアカウントをお持ちの方」→ ログインリンク

**認証フロー:**
```
1. 投稿ボタンクリック
2. 未ログイン判定 → `/auth/signin?callbackUrl=/post/new` にリダイレクト
3. ログイン成功 → callbackUrl に戻る
4. 投稿処理続行
```

---

## デザインシステム

### カラーパレット（TailwindCSS設定）

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        zen: {
          bg: '#F5F1E8',           // 背景（ベージュ）
          card: '#FFFFFF',          // カード背景
          text: {
            primary: '#2D2D2D',     // メインテキスト
            secondary: '#6B6B6B',   // サブテキスト
            muted: '#A0A0A0'        // 薄いテキスト
          },
          accent: {
            red: '#D32F2F',         // アクセント（再生ボタン）
            wood: '#8B6F47',        // 木目調
            gold: '#C9A961'         // ゴールド（バッジ）
          },
          border: '#E5E5E5',        // ボーダー
          divider: '#F0F0F0'        // 区切り線
        }
      },
      fontFamily: {
        ja: ['Noto Serif JP', 'serif'],
        en: ['Playfair Display', 'serif'],
        sans: ['Inter', 'sans-serif']
      },
      borderRadius: {
        'card': '16px',
        'button': '12px'
      },
      boxShadow: {
        'card': '0 2px 8px rgba(0, 0, 0, 0.08)',
        'card-hover': '0 4px 16px rgba(0, 0, 0, 0.12)'
      }
    }
  }
}
```

### タイポグラフィ

```css
/* globals.css */
.heading-large {
  @apply text-4xl font-bold font-ja;
}

.heading-medium {
  @apply text-2xl font-bold font-ja;
}

.body-text {
  @apply text-base font-sans text-zen-text-primary;
}

.caption {
  @apply text-sm font-sans text-zen-text-secondary;
}

.vertical-text {
  writing-mode: vertical-rl;
  @apply font-ja text-lg;
}
```

---

## モックデータ（Week 1用）

### data/tricks.json

30技のサンプルデータを作成します（各カテゴリー 4-5技）。

```json
[
  {
    "id": "1",
    "slug": "big-cup",
    "nameJa": "大皿",
    "nameEn": "Big Cup",
    "category": "大皿系",
    "subcategory": "Basic Catch",
    "difficulty": 1,
    "difficultyLabel": "初級",
    "attribute": "基本(Basic)",
    "thumbnailUrl": "/images/tricks/big-cup.jpg",
    "iconUrl": "/icons/cup.svg",
    "tags": ["初心者向け", "基本技"],
    "videos": [
      {
        "id": "v1",
        "trickId": "1",
        "userId": "u1",
        "username": "kendama_pro",
        "userAvatar": "/avatars/user1.jpg",
        "videoUrl": "https://www.youtube.com/embed/xxxxx",
        "videoType": "youtube",
        "thumbnailUrl": "/thumbnails/v1.jpg",
        "comment": "初心者でも簡単にできます！",
        "views": 1234,
        "likes": 89,
        "createdAt": "2026-01-15T10:00:00Z"
      }
    ],
    "createdAt": "2026-01-01T00:00:00Z"
  },
  {
    "id": "2",
    "slug": "lighthouse",
    "nameJa": "灯台",
    "nameEn": "Lighthouse",
    "category": "灯台系",
    "subcategory": "Balance",
    "difficulty": 3,
    "difficultyLabel": "中級",
    "attribute": "静止(Balance)",
    "thumbnailUrl": "/images/tricks/lighthouse.jpg",
    "iconUrl": "/icons/lighthouse.svg",
    "tags": ["中級者向け", "バランス技"],
    "videos": [
      {
        "id": "v2",
        "trickId": "2",
        "userId": "u2",
        "username": "daily_slayer",
        "userAvatar": "/avatars/user2.jpg",
        "videoUrl": "https://www.youtube.com/embed/yyyyy",
        "videoType": "youtube",
        "thumbnailUrl": "/thumbnails/v2.jpg",
        "views": 2345,
        "likes": 156,
        "createdAt": "2026-01-20T14:30:00Z"
      },
      {
        "id": "v3",
        "trickId": "2",
        "userId": "u3",
        "username": "trick_master",
        "userAvatar": "/avatars/user3.jpg",
        "videoUrl": "https://www.youtube.com/embed/zzzzz",
        "videoType": "youtube",
        "thumbnailUrl": "/thumbnails/v3.jpg",
        "views": 3456,
        "likes": 234,
        "createdAt": "2026-01-22T09:15:00Z"
      }
    ],
    "createdAt": "2026-01-01T00:00:00Z"
  }
  // ... 残り28技
]
```

---

## 実装優先順位

### Day 1-2: プロジェクト初期化 + 基本レイアウト

- [ ] Next.js 15プロジェクト作成
- [ ] TailwindCSS設定
- [ ] next-intl設定（日本語・英語）
- [ ] フォルダ構造作成
- [ ] Header, BottomNav, Footer コンポーネント
- [ ] モックデータ作成

### Day 3-4: 技一覧 + 詳細ページ

- [ ] ホームページ（技一覧グリッド）
- [ ] CategoryTabs コンポーネント
- [ ] TrickCard コンポーネント
- [ ] 技詳細ページ（VideoGrid）
- [ ] VideoCard コンポーネント

### Day 5: 検索機能

- [ ] 技検索ページ
- [ ] オートコンプリート機能
- [ ] 検索ロジック実装

### Day 6: 投稿機能 + 認証

- [ ] NextAuth.js設定（Google OAuth）
- [ ] ログイン/新規登録ページ
- [ ] 新技登録フォーム
- [ ] フォームバリデーション（Zod）
- [ ] 投稿成功ページ

### Day 7: 統合テスト + 調整

- [ ] 全ページの動作確認
- [ ] レスポンシブ対応確認
- [ ] エラーハンドリング確認
- [ ] ローディング状態確認
- [ ] 多言語切り替え確認

---

## 環境変数（.env.local）

```bash
# NextAuth.js
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Week 2で追加予定
# DATABASE_URL=postgresql://...
# AWS_S3_BUCKET=...
```

---

## Week 2への移行計画

Week 1完了後、以下を追加：

1. **Goバックエンド接続**
   - PostgreSQLデータベース
   - REST API実装
   - モックデータからAPI呼び出しに移行

2. **動画アップロード**
   - AWS S3統合
   - FFmpeg動画処理
   - 独自動画ホスティング

3. **SNSシェア機能**
   - Instagram, TikTok, X連携
   - OGP画像生成

4. **コミュニティ機能**
   - いいね・コメント
   - ブックマーク
   - プロフィールページ

5. **ゲーミフィケーション**
   - バッジシステム
   - チャレンジ機能
   - リーダーボード

---

## 技術的考慮事項

### SEO対策（Week 1から）

```typescript
// app/tricks/[slug]/page.tsx
export async function generateMetadata({ params }) {
  const trick = await getTrick(params.slug);

  return {
    title: `${trick.nameJa} (${trick.nameEn}) - Zen Kendama`,
    description: `${trick.nameJa}の剣玉技を学ぼう。複数の実演動画で詳しく解説。難易度: ${trick.difficultyLabel}`,
    openGraph: {
      title: `${trick.nameJa} - 剣玉技辞典`,
      description: `${trick.videos.length}人の実演動画を見る`,
      images: [trick.thumbnailUrl]
    },
    alternates: {
      canonical: `https://kendama-dict.com/tricks/${trick.slug}`,
      languages: {
        'ja': `https://kendama-dict.com/tricks/${trick.slug}`,
        'en': `https://kendama-dict.com/en/tricks/${trick.slug}`
      }
    }
  };
}
```

### パフォーマンス最適化

- Next.js Image コンポーネント使用
- 動的インポート（React.lazy）
- サーバーコンポーネント優先
- クライアントコンポーネントは必要最小限

### アクセシビリティ

- セマンティックHTML
- ARIAラベル
- キーボードナビゲーション
- スクリーンリーダー対応

---

## まとめ

この仕様書に従ってVibe Codingで開発すれば、Week 1（5-7日）でMVPが完成します。

**次のアクション:**
1. 技マスターデータ（30技）の作成
2. Vibe Codingプロンプトの作成
3. Claude Codeで開発開始 🚀
